import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: '#8155a6',
        },
        secondary: {
            main: '#b2102f',
        },
        white: {
            main: '#ffff',
        },
    }
})

export default theme;